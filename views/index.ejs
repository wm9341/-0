<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>鹭云集团活动平台</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/public/style.css">
</head>
<body>
    <header>
        <div class="container">
            <div class="header-content">
                <h1 class="site-title">鹭云集团活动平台</h1>
                <nav class="main-nav">
                    <a href="/" class="nav-link active">活动列表</a>
                    <% if (user) { %>
                        <span class="welcome-message">欢迎，<%= user.username %></span>
                        <% if (user.isAdmin) { %>
                            <a href="/admin" class="nav-link">管理后台</a>
                        <% } %>
                        <a href="/logout" class="nav-link">退出登录</a>
                    <% } else { %>
                        <a href="/login" class="nav-link">登录</a>
                        <a href="/register" class="nav-link">注册</a>
                    <% } %>
                </nav>
            </div>
        </div>
    </header>
    
    <div class="container">
        <div class="page-header">
            <h2 class="page-title">最新活动</h2>
            <p class="page-description">参与我们的航空活动，体验飞行的魅力</p>
        </div>
        
        <% if (events && events.length === 0) { %>
            <div class="no-events">
                <p>暂无活动，敬请期待！</p>
            </div>
        <% } else if (events) { %>
            <div class="events-grid">
                <% for (var i = 0; i < events.length; i++) { %>
                <% var event = events[i]; %>
                <div class="event-card">
                    <div class="event-card-header">
                        <h3 class="event-title"><%= event.title %></h3>
                        <span class="event-date"><%= new Date(event.startTime).toLocaleDateString() %></span>
                    </div>
                    <div class="event-card-body">
                        <div class="event-info-item">
                            <i class="fas fa-calendar"></i>
                            <span><%= new Date(event.startTime).toLocaleString() %></span>
                        </div>
                        <div class="event-info-item">
                            <i class="fas fa-plane"></i>
                            <span><%= event.departure %> → <%= event.arrival %></span>
                        </div>
                        <% if (event.aircraftTypes && event.aircraftTypes.length > 0) { %>
                            <div class="event-info-item">
                                <i class="fas fa-building"></i>
                                <span><%= event.aircraftTypes.join(', ') %></span>
                            </div>
                        <% } %>
                    </div>
                    <div class="event-card-footer">
                        <a href="/event/<%= event.id %>" class="view-details-btn">查看详情</a>
                    </div>
                </div>
                <% } %>
            </div>
        <% } %>
    </div>
    

</body>
</html>